// program to find if one eligible to vote
#include <stdio.h>
int main()
 {
int age,citizenship;
    printf("\nEnter the  age and citizenship");
scanf("%d,%d",&age,&citizenship);
if(age>18 && citizenship);
{printf("eligible to vote");}
{
printf("not eligible to vote");}
    return 0;
}